#include "main.h"
#include <string.h>
/**
* _string_toupper - copy a string
*@*
* Return: the pointer to dest
*/
char *string_toupper(char *)
{
	int num1, num2;

	num2 = strlen(src);
	for (num1 = 0; num1 < num2; num1++)
	{
		dest[num1] = src[num1];
	}
	dest[num1++] = '\0';
	return (dest);
}

